export type Partner = {
    id: number;
    name: string;
    logo: string;
    link: string;
}

export type NewsItem = {
    id: number
    title: string
    description: string
    imageUrl: string
  }